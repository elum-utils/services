version: "2"
sql:
  - engine: "mysql"
    schema: "schema.sql"
    queries: "query.sql"
    gen:
      go:
        package: "sqlc"
        out: "sqlc"
        emit_json_tags: true
        emit_prepared_queries: true
